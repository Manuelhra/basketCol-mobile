/**
 * @format
 */

import {AppRegistry} from 'react-native';

import { BasketColApp } from './src/BasketColApp';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => BasketColApp);
