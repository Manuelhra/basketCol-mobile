import { authenticationEndpoints } from '../../../authentication/api/endpoints';
import { IEndpoints } from './IEndpoints';

export const endpoints: IEndpoints = {
  authentication: authenticationEndpoints,
};
