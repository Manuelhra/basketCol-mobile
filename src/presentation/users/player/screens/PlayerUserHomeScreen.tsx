import React from 'react';
import { Text, View } from 'react-native';

type PlayerUserHomeScreenProps = {};

export const PlayerUserHomeScreen = ({}: PlayerUserHomeScreenProps): React.JSX.Element => {
  return (
    <View>
      <Text>Player User Home Screen</Text>
    </View>
  );
};
